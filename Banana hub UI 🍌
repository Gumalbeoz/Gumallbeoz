--[[
    BANANA HUB - AUTO FARM TỔNG HỢP TẤT CẢ TÍNH NĂNG
    Một nút Auto Farm duy nhất tự động kích hoạt:
    1. Bay lên đầu quái
    2. Tấn công không animation (Fast Attack No Animation)
    3. Bring Mob - Kéo tất cả quái về vị trí
    4. Tự động chọn vũ khí tầm xa nhất
    5. Tăng tầm đánh và tốc độ đánh
    
    Tác giả: palofsc
    Tích hợp hoàn chỉnh vào Banana Hub
]]

-- Kiểm tra và tạo Window nếu chưa tồn tại
if not Window then
    local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
    Window = Rayfield:CreateWindow({
       Name = "Banana Hub | Blox Fruits",
       LoadingTitle = "Banana Hub",
       LoadingSubtitle = "by palofsc",
       ConfigurationSaving = {
          Enabled = true,
          FolderName = "BananaHub_Config",
          FileName = "BananaHub_Settings"
       },
       Discord = {
          Enabled = false,
          Invite = "",
          RememberJoins = true
       },
       KeySystem = false,
       KeySettings = {
          Title = "Key",
          Subtitle = "Nhập Key",
          Note = "Nhập key để sử dụng",
          FileName = "BananaHubKey",
          SaveKey = true,
          GrabKeyFromSite = false,
          Key = {"BananaHub2024"}
       }
    })
end

-- Tab Auto Farm Tổng Hợp
local AutoFarmTab = Window:CreateTab("🏠 Auto Farm", 4483362458)

-- Section Cấu hình Auto Farm Tổng Hợp
local ConfigSection = AutoFarmTab:CreateSection("Cấu hình Auto Farm Tổng Hợp")

-- Slider bán kính quét quái
local FarmRadiusSlider = AutoFarmTab:CreateSlider({
   Name = "Bán kính quét quái",
   Range = {50, 1000},
   Increment = 10,
   Suffix = "Studs",
   CurrentValue = 300,
   Flag = "FarmRadius",
   Callback = function(Value)
       _G.FarmRadius = Value
   end
})

-- Slider độ cao bay trên đầu quái
local FlyHeightSlider = AutoFarmTab:CreateSlider({
   Name = "Độ cao bay trên đầu quái",
   Range = {3, 50},
   Increment = 1,
   Suffix = "Studs",
   CurrentValue = 15,
   Flag = "FlyHeight",
   Callback = function(Value)
       _G.FlyHeightOffset = Value
   end
})

-- Slider tầm đánh mở rộng
local AttackRangeSlider = AutoFarmTab:CreateSlider({
   Name = "Hệ số mở rộng tầm đánh",
   Range = {1, 15},
   Increment = 0.5,
   Suffix = "x",
   CurrentValue = 5,
   Flag = "AttackRangeMulti",
   Callback = function(Value)
       _G.AttackRangeMultiplier = Value
   end
})

-- Slider tốc độ đánh
local AttackSpeedSlider = AutoFarmTab:CreateSlider({
   Name = "Tốc độ đánh (độ trễ)",
   Range = {0.0001, 0.1},
   Increment = 0.0001,
   Suffix = "s",
   CurrentValue = 0.001,
   Flag = "AttackSpeedDelay",
   Callback = function(Value)
       _G.AttackSpeedDelay = Value
   end
})

-- Slider số đòn đánh thêm mỗi tick
local ExtraHitsSlider = AutoFarmTab:CreateSlider({
   Name = "Số đòn đánh thêm mỗi tick",
   Range = {1, 30},
   Increment = 1,
   Suffix = "đòn",
   CurrentValue = 10,
   Flag = "ExtraHitsPerTick",
   Callback = function(Value)
       _G.ExtraHitsPerTick = Value
   end
})

-- Slider tốc độ kéo quái
local PullSpeedSlider = AutoFarmTab:CreateSlider({
   Name = "Tốc độ kéo quái",
   Range = {10, 500},
   Increment = 10,
   Suffix = "Studs/s",
   CurrentValue = 100,
   Flag = "PullSpeed",
   Callback = function(Value)
       _G.PullSpeed = Value
   end
})

-- Section Toggle phụ trợ
local ToggleSection = AutoFarmTab:CreateSection("Tùy chọn bổ sung")

-- Toggle bật/tắt vô hiệu AI quái
local DisableAIToggle = AutoFarmTab:CreateToggle({
   Name = "Vô hiệu AI quái (đứng yên)",
   CurrentValue = true,
   Flag = "DisableMobAI",
   Callback = function(Value)
       _G.DisableMobAI = Value
   end
})

-- Toggle bật/tắt tự động nhặt đồ
local AutoCollectToggle = AutoFarmTab:CreateToggle({
   Name = "Tự động nhặt đồ",
   CurrentValue = true,
   Flag = "AutoCollectLoot",
   Callback = function(Value)
       _G.AutoCollectLoot = Value
   end
})

-- Toggle bật/tắt chống AFK
local AntiAFKToggle = AutoFarmTab:CreateToggle({
   Name = "Chống AFK",
   CurrentValue = true,
   Flag = "AntiAFK",
   Callback = function(Value)
       _G.AntiAFK = Value
       if _G.AntiAFK then
           spawn(function()
               local vu = game:GetService("VirtualUser")
               game.Players.LocalPlayer.Idled:Connect(function()
                   vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
                   wait(1)
                   vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
               end)
           end)
       end
   end
})

-- ============================================
-- NÚT AUTO FARM DUY NHẤT - TÍCH HỢP TẤT CẢ
-- ============================================
local MasterAutoFarmToggle = AutoFarmTab:CreateToggle({
   Name = "⚡ AUTO FARM TỔNG HỢP (Bay + No Anim + Bring Mob)",
   CurrentValue = false,
   Flag = "MasterAutoFarm",
   Callback = function(Value)
       _G.MasterAutoFarm = Value
       
       if _G.MasterAutoFarm then
           -- ============================================
           -- KHỞI ĐỘNG TẤT CẢ CÁC LUỒNG CÙNG LÚC
           -- ============================================
           
           -- Luồng 1: Bay lên đầu quái + Bring Mob + Di chuyển
           spawn(function()
               while _G.MasterAutoFarm do
                   pcall(function()
                       local player = game.Players.LocalPlayer
                       local character = player.Character or player.CharacterAdded:Wait()
                       local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
                       local humanoid = character:WaitForChild("Humanoid")
                       
                       -- Lấy vị trí người chơi
                       local playerPosition = humanoidRootPart.Position
                       local farmRadius = _G.FarmRadius or 300
                       local flyHeight = _G.FlyHeightOffset or 15
                       local pullSpeed = _G.PullSpeed or 100
                       
                       -- Tìm quái gần nhất để bay đến
                       local nearestEnemy = nil
                       local nearestDistance = math.huge
                       local allEnemiesInRange = {}
                       
                       for _, enemy in pairs(game.Workspace.Enemies:GetChildren()) do
                           if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
                               if enemy:FindFirstChild("HumanoidRootPart") then
                                   local enemyPosition = enemy.HumanoidRootPart.Position
                                   local distance = (playerPosition - enemyPosition).Magnitude
                                   
                                   if distance <= farmRadius then
                                       -- Thêm vào danh sách quái trong phạm vi
                                       table.insert(allEnemiesInRange, enemy)
                                       
                                       -- Vô hiệu AI quái nếu bật
                                       if _G.DisableMobAI then
                                           pcall(function()
                                               enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Running, false)
                                               enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, false)
                                               enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, false)
                                               enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, false)
                                               enemy.Humanoid.WalkSpeed = 0
                                               enemy.Humanoid.JumpPower = 0
                                           end)
                                       end
                                       
                                       -- BRING MOB: Kéo quái về phía người chơi
                                       if distance > 15 then
                                           -- Sử dụng BodyVelocity để kéo quái
                                           local bodyVelocity = enemy.HumanoidRootPart:FindFirstChild("PullVelocity")
                                           if not bodyVelocity then
                                               bodyVelocity = Instance.new("BodyVelocity")
                                               bodyVelocity.Name = "PullVelocity"
                                               bodyVelocity.MaxForce = Vector3.new(1, 1, 1) * math.huge
                                               bodyVelocity.P = 100000
                                               bodyVelocity.Parent = enemy.HumanoidRootPart
                                           end
                                           
                                           -- Tính hướng kéo về người chơi
                                           local direction = (playerPosition - enemyPosition).Unit
                                           local velocity = direction * pullSpeed
                                           velocity = Vector3.new(velocity.X, flyHeight * 3, velocity.Z)
                                           bodyVelocity.Velocity = velocity
                                       else
                                           -- Nếu đã gần, xóa BodyVelocity để quái đứng yên
                                           if enemy.HumanoidRootPart:FindFirstChild("PullVelocity") then
                                               enemy.HumanoidRootPart.PullVelocity:Destroy()
                                           end
                                           enemy.HumanoidRootPart.Velocity = Vector3.new(0, 0, 0)
                                       end
                                       
                                       -- Tìm quái gần nhất để bay đến
                                       if distance < nearestDistance then
                                           nearestDistance = distance
                                           nearestEnemy = enemy
                                       end
                                   else
                                       -- Xóa BodyVelocity cho quái ngoài phạm vi
                                       if enemy.HumanoidRootPart:FindFirstChild("PullVelocity") then
                                           enemy.HumanoidRootPart.PullVelocity:Destroy()
                                       end
                                   end
                               end
                           end
                       end
                       
                       -- BAY LÊN ĐẦU QUÁI GẦN NHẤT
                       if nearestEnemy then
                           -- Kích hoạt bay
                           if not humanoidRootPart:FindFirstChild("FlyVelocity") then
                               local bodyVelocity = Instance.new("BodyVelocity")
                               bodyVelocity.Name = "FlyVelocity"
                               bodyVelocity.MaxForce = Vector3.new(1, 1, 1) * math.huge
                               bodyVelocity.P = 100000
                               bodyVelocity.Parent = humanoidRootPart
                           end
                           
                           if not humanoidRootPart:FindFirstChild("FlyGyro") then
                               local bodyGyro = Instance.new("BodyGyro")
                               bodyGyro.Name = "FlyGyro"
                               bodyGyro.MaxTorque = Vector3.new(1, 1, 1) * math.huge
                               bodyGyro.CFrame = humanoidRootPart.CFrame
                               bodyGyro.Parent = humanoidRootPart
                           end
                           
                           -- Bay đến vị trí trên đầu quái gần nhất
                           local targetFlyPosition = nearestEnemy.HumanoidRootPart.Position + Vector3.new(0, flyHeight, 0)
                           local flyVelocity = humanoidRootPart:FindFirstChild("FlyVelocity")
                           if flyVelocity then
                               local directionToTarget = (targetFlyPosition - humanoidRootPart.Position).Unit
                               local distanceToTarget = (targetFlyPosition - humanoidRootPart.Position).Magnitude
                               flyVelocity.Velocity = directionToTarget * math.min(distanceToTarget * 10, 200)
                           end
                           
                           -- Cập nhật BodyGyro để nhìn xuống
                           local flyGyro = humanoidRootPart:FindFirstChild("FlyGyro")
                           if flyGyro then
                               flyGyro.CFrame = CFrame.new(
                                   humanoidRootPart.Position,
                                   nearestEnemy.HumanoidRootPart.Position
                               )
                           end
                           
                           -- Dịch chuyển nhanh nếu cần (khi quá xa)
                           if nearestDistance > 100 then
                               humanoidRootPart.CFrame = CFrame.new(
                                   targetFlyPosition,
                                   nearestEnemy.HumanoidRootPart.Position
                               )
                           end
                       end
                   end)
                   wait(0.01)
               end
               
               -- Khi tắt: dọn dẹp
               pcall(function()
                   local player = game.Players.LocalPlayer
                   local character = player.Character
                   if character then
                       local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
                       if humanoidRootPart then
                           if humanoidRootPart:FindFirstChild("FlyVelocity") then
                               humanoidRootPart.FlyVelocity:Destroy()
                           end
                           if humanoidRootPart:FindFirstChild("FlyGyro") then
                               humanoidRootPart.FlyGyro:Destroy()
                           end
                       end
                   end
                   -- Dọn BodyVelocity quái
                   for _, enemy in pairs(game.Workspace.Enemies:GetChildren()) do
                       if enemy:FindFirstChild("HumanoidRootPart") then
                           if enemy.HumanoidRootPart:FindFirstChild("PullVelocity") then
                               enemy.HumanoidRootPart.PullVelocity:Destroy()
                           end
                           if enemy:FindFirstChild("Humanoid") then
                               enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Running, true)
                               enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, true)
                               enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
                               enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, true)
                               enemy.Humanoid.WalkSpeed = 16
                               enemy.Humanoid.JumpPower = 50
                           end
                       end
                   end
               end)
           end)
           
           -- Luồng 2: Fast Attack No Animation + Tăng tầm đánh
           spawn(function()
               -- Vô hiệu hóa animation vĩnh viễn trong luồng này
               local function disableAllAnimations()
                   pcall(function()
                       local player = game.Players.LocalPlayer
                       local character = player.Character
                       if character and character:FindFirstChild("Humanoid") then
                           local humanoid = character.Humanoid
                           local animator = humanoid:FindFirstChild("Animator")
                           if animator then
                               -- Dừng tất cả animation đang chạy
                               for _, track in pairs(animator:GetPlayingAnimationTracks()) do
                                   track:Stop(0)
                               end
                           end
                           -- Vô hiệu tất cả state
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Running, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.RunningNoPhysics, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.GettingUp, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Landed, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Freefall, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.PlatformStanding, false)
                       end
                   end)
               end
               
               local lastAttackTime = 0
               
               while _G.MasterAutoFarm do
                   pcall(function()
                       local player = game.Players.LocalPlayer
                       local character = player.Character
                       if not character then return end
                       
                       local humanoid = character:FindFirstChild("Humanoid")
                       if not humanoid then return end
                       
                       -- Tự động chọn và trang bị vũ khí tầm xa nhất
                       local bestTool = nil
                       local bestRange = 0
                       
                       for _, tool in pairs(character:GetChildren()) do
                           if tool:IsA("Tool") and tool:FindFirstChild("Handle") then
                               local toolRange = tool.Handle.Size.Magnitude
                               if toolRange > bestRange then
                                   bestRange = toolRange
                                   bestTool = tool
                               end
                           end
                       end
                       
                       -- Trang bị vũ khí tốt nhất
                       if bestTool and character:FindFirstChildOfClass("Tool") ~= bestTool then
                           humanoid:EquipTool(bestTool)
                       end
                       
                       -- Mở rộng hitbox vũ khí
                       local currentTool = character:FindFirstChildOfClass("Tool")
                       if currentTool and currentTool:FindFirstChild("Handle") then
                           local handle = currentTool.Handle
                           local multiplier = _G.AttackRangeMultiplier or 5
                           handle.Size = Vector3.new(
                               handle.Size.X * multiplier,
                               handle.Size.Y * multiplier,
                               handle.Size.Z * multiplier
                           )
                           handle.Transparency = 0.7
                           handle.Massless = true
                           handle.CanCollide = false
                       end
                       
                       -- Vô hiệu animation
                       disableAllAnimations()
                       
                       -- Tấn công tất cả quái trong phạm vi
                       local attackDelay = _G.AttackSpeedDelay or 0.001
                       local currentTime = tick()
                       
                       if currentTime - lastAttackTime >= attackDelay then
                           local extraHits = _G.ExtraHitsPerTick or 10
                           
                           for hit = 1, extraHits do
                               for _, enemy in pairs(game.Workspace.Enemies:GetChildren()) do
                                   if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
                                       if enemy:FindFirstChild("HumanoidRootPart") then
                                           -- Tấn công qua remote
                                           pcall(function()
                                               game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(
                                                   "Hit",
                                                   "Melee",
                                                   enemy
                                               )
                                               game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(
                                                   "Hit",
                                                   "Sword",
                                                   enemy
                                               )
                                           end)
                                       end
                                   end
                               end
                               
                               -- Kích hoạt tool để tấn công
                               if currentTool then
                                   currentTool:Activate()
                               end
                           end
                           
                           lastAttackTime = currentTime
                       end
                   end)
                   wait(0.0001)
               end
               
               -- Khôi phục animation khi tắt
               pcall(function()
                   local player = game.Players.LocalPlayer
                   local character = player.Character
                   if character then
                       local humanoid = character:FindFirstChild("Humanoid")
                       if humanoid then
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Running, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.RunningNoPhysics, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.GettingUp, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Landed, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Freefall, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.PlatformStanding, true)
                       end
                   end
               end)
           end)
           
           -- Luồng 3: Tự động nhặt đồ
           spawn(function()
               while _G.MasterAutoFarm do
                   if _G.AutoCollectLoot then
                       pcall(function()
                           local player = game.Players.LocalPlayer
                           local character = player.Character
                           if character then
                               local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
                               if humanoidRootPart then
                                   for _, item in pairs(game.Workspace:GetChildren()) do
                                       if item:IsA("Tool") or item.Name:lower():find("drop") or item.Name:lower():find("chest") then
                                           local distance = (humanoidRootPart.Position - item.Position).Magnitude
                                           if distance <= 50 then
                                               firetouchinterest(item, humanoidRootPart, 0)
                                               firetouchinterest(item, humanoidRootPart, 1)
                                           end
                                       end
                                   end
                               end
                           end
                       end)
                   end
                   wait(0.1)
               end
           end)
           
       else
           -- KHI TẮT: Dọn dẹp tất cả
           spawn(function()
               pcall(function()
                   local player = game.Players.LocalPlayer
                   local character = player.Character
                   if character then
                       local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
                       if humanoidRootPart then
                           -- Xóa fly components
                           if humanoidRootPart:FindFirstChild("FlyVelocity") then
                               humanoidRootPart.FlyVelocity:Destroy()
                           end
                           if humanoidRootPart:FindFirstChild("FlyGyro") then
                               humanoidRootPart.FlyGyro:Destroy()
                           end
                       end
                       
                       -- Khôi phục humanoid state
                       local humanoid = character:FindFirstChild("Humanoid")
                       if humanoid then
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Running, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.RunningNoPhysics, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.GettingUp, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Landed, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Freefall, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                           humanoid:SetStateEnabled(Enum.HumanoidStateType.PlatformStanding, true)
                       end
                   end
                   
                   -- Dọn dẹp tất cả BodyVelocity của quái
                   for _, enemy in pairs(game.Workspace.Enemies:GetChildren()) do
                       if enemy:FindFirstChild("HumanoidRootPart") then
                           if enemy.HumanoidRootPart:FindFirstChild("PullVelocity") then
                               enemy.HumanoidRootPart.PullVelocity:Destroy()
                           end
                           enemy.HumanoidRootPart.Anchored = false
                           enemy.HumanoidRootPart.Velocity = Vector3.new(0, 0, 0)
                       end
                       if enemy:FindFirstChild("Humanoid") then
                           enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Running, true)
                           enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, true)
                           enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
                           enemy.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, true)
                           enemy.Humanoid.WalkSpeed = 16
                           enemy.Humanoid.JumpPower = 50
                       end
                   end
               end)
           end)
       end
   end
})

-- Section Trạng thái
local StatusSection = AutoFarmTab:CreateSection("Trạng thái hiện tại")

-- Nút kiểm tra trạng thái
local CheckStatusButton = AutoFarmTab:CreateButton({
   Name = "Kiểm tra trạng thái Auto Farm",
   Callback = function()
       local status = "Auto Farm Tổng Hợp: " .. tostring(_G.MasterAutoFarm and "ĐANG CHẠY" or "ĐÃ TẮT") .. "\n"
       status = status .. "Bay trên đầu quái: " .. tostring(_G.MasterAutoFarm and "✔️" or "❌") .. "\n"
       status = status .. "Fast Attack No Anim: " .. tostring(_G.MasterAutoFarm and "✔️" or "❌") .. "\n"
       status = status .. "Bring Mob: " .. tostring(_G.MasterAutoFarm and "✔️" or "❌") .. "\n"
       status = status .. "Tầm đánh: " .. tostring(_G.AttackRangeMultiplier or 5) .. "x\n"
       status = status .. "Tốc độ đánh: " .. tostring(_G.AttackSpeedDelay or 0.001) .. "s\n"
       status = status .. "Bán kính farm: " .. tostring(_G.FarmRadius or 300) .. " Studs\n"
       status = status .. "Độ cao bay: " .. tostring(_G.FlyHeightOffset or 15) .. " Studs"
       
       local Notification = loadstring(game:HttpGet("https://api.voidscripts.com/UI/Notify.lua"))()
       Notification.new("info", status, 8)
   end
})

-- Nút dừng khẩn cấp
local EmergencyStopButton = AutoFarmTab:CreateButton({
   Name = "🛑 DỪNG KHẨN CẤP TẤT CẢ",
   Callback = function()
       _G.MasterAutoFarm = false
       pcall(function()
           local player = game.Players.LocalPlayer
           local character = player.Character
           if character then
               local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
               if humanoidRootPart then
                   if humanoidRootPart:FindFirstChild("FlyVelocity") then
                       humanoidRootPart.FlyVelocity:Destroy()
                   end
                   if humanoidRootPart:FindFirstChild("FlyGyro") then
                       humanoidRootPart.FlyGyro:Destroy()
                   end
               end
               local humanoid = character:FindFirstChild("Humanoid")
               if humanoid then
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.Running, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.RunningNoPhysics, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.GettingUp, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.Landed, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.Freefall, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                   humanoid:SetStateEnabled(Enum.HumanoidStateType.PlatformStanding, true)
               end
           end
           for _, enemy in pairs(game.Workspace.Enemies:GetChildren()) do
               if enemy:FindFirstChild("HumanoidRootPart") then
                   if enemy.HumanoidRootPart:FindFirstChild("PullVelocity") then
                       enemy.HumanoidRootPart.PullVelocity:Destroy()
                   end
                   enemy.HumanoidRootPart.Anchored = false
               end
           end
       end)
       local Notification = loadstring(game:HttpGet("https://api.voidscripts.com/UI/Notify.lua"))()
       Notification.new("warning", "Đã dừng tất cả Auto Farm!", 3)
   end
})

-- Cập nhật cấu hình
Rayfield:LoadConfiguration()

-- Thông báo hoàn tất
local Notification = loadstring(game:HttpGet("https://api.voidscripts.com/UI/Notify.lua"))()
Notification.new("success", "Banana Hub - Auto Farm Tổng Hợp đã sẵn sàng!", 5)

print("========================================")
print("  BANANA HUB - AUTO FARM TỔNG HỢP")
print("  Bay + No Animation + Bring Mob")
print("  Tất cả trong MỘT NÚT duy nhất!")
print("  by palofsc")
print("========================================")
print("Hướng dẫn: Chỉ cần bật 'AUTO FARM TỔNG HỢP'")
print("Tất cả tính năng sẽ tự động kích hoạt!") 
